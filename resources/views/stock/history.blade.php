@extends('layouts.stockify') @section('content')<div class="flex flex-wrap justify-between gap-3"><div><h1 class="text-3xl font-bold">Stok & Transaksi</h1><p class="text-slate-500">Catatan barang masuk, keluar, dan opname.</p></div><div class="flex gap-2"><a class="rounded bg-emerald-600 px-3 py-2 text-white" href="{{route('stock.in.create')}}">+ Masuk</a><a class="rounded bg-amber-500 px-3 py-2 text-white" href="{{route('stock.out.create')}}">+ Keluar</a><a class="rounded bg-slate-800 px-3 py-2 text-white" href="{{route('stock.opname.create')}}">Opname</a></div></div><div class="mt-6 grid gap-5 lg:grid-cols-3">@foreach([['Barang Masuk',$ins,'in'],['Barang Keluar',$outs,'out'],['Stock Opname',$opnames,'opname']] as [$title,$items,$type])<section class="rounded-xl bg-white p-5 shadow"><h2 class="font-bold">{{$title}}</h2>@forelse($items as $item)<div class="mt-3 border-b pb-2"><b>{{$item->product->name}}</b><p class="text-sm text-slate-500">{{$item->date->format('d/m/Y')}} · {{$type==='opname'?($item->system_stock.' → '.$item->physical_stock):$item->quantity}}</p></div>@empty<p class="mt-3 text-slate-500">Belum ada data.</p>@endforelse</section>@endforeach</div>@endsection
